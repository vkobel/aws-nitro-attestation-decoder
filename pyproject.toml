[project]
name = "turnkey-discovery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cbor2>=5.7.1",
    "cryptography>=46.0.3",
]
